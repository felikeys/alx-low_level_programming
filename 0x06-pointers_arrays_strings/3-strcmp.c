#include "main.h"
#include <string.h>

/**
* _strcmp - This function returns 0 when strings
* are the same
* @s1: An input string
* @s2: An input string
* Return: An integer
*/

int _strcmp(char *s1, char *s2)
{
	int r;

	r = strcmp(s1, s2);
	return (r);

}
